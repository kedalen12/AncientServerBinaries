namespace Core
{
    public static class Enums 
    {
        public enum LobbyEndReason
        {
            PlayerDidNotSelect,
            PlayerDisconnected,
            ServerEnded
        }
        public enum MapType
        {
            AztecMap,
            MysteriousMists
        }
    }
}
